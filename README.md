# constructer-copy-constructer.cpp
only use for  tourbo c++

#include<iostream>
#include<conio.h>
class max
{ 
private:
      max()
      {
      x=0;
      y=0;
      }
      max(int a,int b)
     {
     x=a;
     y=b;
     }
     void show()
     {
     cout<<"\n x="<<x;
   cout<<"\n y="<<y;
   }
   };
   main()
   {
   max m1;
   m1.show();
   max.m2(50,60);
   m2.show();
   getch ();
   }
     
